# 梨ママ（サブ垢）専用プロジェクト

このディレクトリは **梨ママ = `mama_fukugyo_nikki`（表示名「梨🍐ママ副業日記📓💫」）** 専用です。
このトークでは梨ママの作業だけを扱います。

## もう一つのアカウントには触らない
**楓ママ = `peng.uin_mama`（表示名「楓_育児全力ママ」）** は別プロジェクト `C:\Users\treet\threads-affiliate-bot` で、**別のトーク**で運用しています。ユーザーがトークを分けたいと明示的に希望しているため、こちらのトークで楓ママ側のファイルを編集したり投稿したりしないこと。楓ママの話題が出たら、そちらのトークでやる方がいい旨を伝える。

2アカウントは**意図的に紐付けを避けています**。梨ママの投稿に地域や、楓ママ側でしか出していない個人情報を書かない（詳細は `fukugyo_prompt.md` のペルソナ項）。

## アカウントの中身
副業（アフィリエイト）の収益記録アカウント。将来的にnote（有料）への導線にする想定。
**投稿方針・ペルソナ・口調・禁止事項はすべて `fukugyo_prompt.md` が正。** 投稿文を書く前に必ず読むこと。

特に重要な絶対ルール（`fukugyo_prompt.md` より）：
- **本文に出す収益・実績の数字は、ユーザーから明示的に伝えられた実数のみ。** 新しい数字が無い回に、金額を創作したり前回の数字を更新して見せたりしない
- 「絶対稼げる」「誰でも簡単に」のような断定的な収益確約は禁止（特定商取引法・景品表示法）
- noteのリンクは、実際にnoteが公開されている場合のみ貼る

## 構成
- `fukugyo_prompt.md` — 投稿方針（自動実行時にこれがそのままプロンプトになる）
- `publish.py` — 投稿CLI。標準入力にJSONを渡す
- `threads_api.py` / `load_env.py` — Threads APIと`.env`読み込み
- `log.jsonl` — 投稿ログ（追記のみ。コンフリクト時は両方の行を残す）
- `note_drafts/` — note記事の下書き。**まだnote.comには公開していない**
- `.github/workflows/daily-post.yml` — 1日1回の自動投稿（cron）

## 投稿のしかた
一時ファイルは作らず、ヒアドキュメントで直接渡す：
```
cat <<'EOF' | python publish.py
{"text": "投稿文", "reply_text": "コメント欄の続き", "topic_tag": "副業"}
EOF
```

## 画像を付けるとき
**生成した画像は必ずユーザーに見せて選んでもらってから投稿する**（A案/B案の2択＋違いを一言で）。勝手に公開しない。

画像はローカルファイルを直接アップロードできず、**公開URLが必要**。楓ママ側はGitHub Pagesの中継リポジトリを使っているが、梨ママ側には現時点でその仕組みが無いので、画像付き投稿をやるならまず置き場所を決めるところから。

画像生成は、ローカルのStable Diffusion WebUI Forge（`http://127.0.0.1:7860`、API有効）が使える。Fluxが構図の指示に強い。

## 現状（2026-09-09時点）
- 自動投稿は稼働済み。`log.jsonl` の実績はまだ1件で、立ち上げ直後の段階
- note記事は下書き2本（自己紹介 / 「売上ゼロ」から最初の179円が生まれるまでにやったこと）が `note_drafts/` にあるが**未公開**
